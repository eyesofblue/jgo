# {{.Name}}

This project was generated by [JGO](https://github.com/eyesofblue/jgo).

- Project type: `{{.Type}}`
- Go module: `{{.Module}}`
- Go version: `{{.GoVersion}}` (JGO minimum: `1.24.0`)

## Run

```bash
go run ./cmd/server --config configs/local.yaml
```

## Check

```bash
make check
```

## List and debug APIs

```bash
jgo doctor
jgo generate
jgo list
jgo run
jgo build
```
{{if .HasWeb}}

## HTTP API

Public request and response structs live in `api/http/model/`. Add and generate an API with:

First define `UserInfo` (and any complex request type) as a Go struct in `api/http/model/`, then run:

```bash
jgo api add GetUser --method GET --path /get_user \
  --request uid:int64:required \
  --response-data UserInfo
jgo api generate

jgo call http GetUser \
  --addr http://127.0.0.1:8080 \
  --data '{"uid":12345}'
```
{{end}}
{{if .HasGRPC}}

## gRPC API

Install the locked Buf toolchain once, add an RPC, edit its protobuf fields, and generate code:

```bash
jgo tools install
jgo rpc add GetUser --service {{.ServiceName}}
# Edit api/proto/{{.PackageName}}/v1/service.proto.
jgo rpc generate

jgo call grpc {{.ServiceName}}.GetUser \
  --addr 127.0.0.1:9090 \
  --data '{}'
```

`jgo rpc generate` overwrites generated protobuf and transport files, but never overwrites an existing business service method.
Generated business methods use `<Service><RPC>` names, such as `{{.ServiceName}}GetUser`, so mixed projects can safely use the same operation name over HTTP and gRPC.
{{end}}
{{if eq .JGOVersion "v0.0.0"}}

## Local JGO development

This project currently references the unreleased placeholder `github.com/eyesofblue/jgo v0.0.0`.
Generate with `--jgo-replace /path/to/jgo`, or change the requirement to a released JGO version.
{{end}}
